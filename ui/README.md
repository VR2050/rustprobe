# UI Shell

This directory contains a minimal frontend scaffold for RustProbe.

Current scope:

- static HTML entry
- lightweight TypeScript bootstrap
- placeholder dashboard layout

Next steps:

- connect to IPC bridge or local WebSocket
- render flows, objects, metrics, and alerts
- add filtering and export UX
